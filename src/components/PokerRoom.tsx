import React from "react";
import styled from "styled-components";

const PokerRoom: React.FC  = (props: any) => {
  return (
    <PokerRoomWrapper>
     11
    </PokerRoomWrapper>
  );
};
export default PokerRoom;

const PokerRoomWrapper = styled.div`
  width: calc(100% / 3);
  height: 3rem;
  display: flex;
  justify-content: space-around;
  align-items: center;
`;
